import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    button: {
        margin: '10px 0'
    },
    formTitle: {
        fontSize: 28,
        padding: 20,
        textAlign: "center",
        color: "#3451b9"
    }
})